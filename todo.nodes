- code:load_binary needs crell, and the remote nodes to have to same mayor version of OTP installed ( fix / find a solution )
- basic javascript input validation.

- Erlang Versions
	- possible create docker containers, that compile the remote source
	  file, each container can be a certain Erlang VSN.
